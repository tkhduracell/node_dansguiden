{
	"apps": [
		{
			"name": "web_dansguiden",
			"script": "bin/www",
			"ignoreWatch": ["node_modules", "public/versions.json"],
			"watch": true,
			"env": {
				"NODE_ENV": "production",
				"PORT": 5000
			},
			"max_memory_restart": "256M",
			"node_args": "--expose-gc"
		}
	]
}
